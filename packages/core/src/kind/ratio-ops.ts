// Moved to `@smartput/kind`; re-exported here for the reason `../decimal.ts` gives.
export {
  BOOLEAN_KIND,
  BOOLEAN_UNIT,
  COMPARE_PRECISION,
  COMPARISON_OPS,
  deriveValue,
  generateComparisonOps,
  generateRatioOps,
  NUMBER_KIND,
  PERCENT_KIND,
} from "@smartput/kind/ratio-ops";
