# @smartput/range-core

> Endpoints, ordering, windows — the machinery every range kind shares.

`wrapRange`/`unwrapRange`, `assertOrdered` and
`InvertedRangeError`, plus `WINDOWS` — the named spans (`this week`, `last
month`) every range kind resolves against. No range kind of its own.

## Setup

```sh
npm add @smartput/range-core
```

## Example

```ts
import { startOfWeek, endOfWeek, endOfMonth } from "@smartput/range-core";
import { Temporal } from "@smartput/datetime";

const now = Temporal.ZonedDateTime.from("2026-01-15T09:30[Europe/Kyiv]");

startOfWeek(now).toString()                    // "2026-01-12T00:00:00+02:00[Europe/Kyiv]"
startOfWeek(now, { weekStart: 7 }).toString()  // "2026-01-11T00:00:00+02:00[Europe/Kyiv]"
endOfWeek(now).toString()                      // "2026-01-19T00:00:00+02:00[Europe/Kyiv]"
endOfMonth(now).toString()                     // "2026-02-01T00:00:00+02:00[Europe/Kyiv]"
```

The window arithmetic every range kind shares, with no kind of its own.
`DEFAULT_WEEK_START` is Monday; `createDateRange({ weekStart: 7 })` is how a
Sunday-first calendar says so.

## Entry points

| Import | Contents |
| --- | --- |
| `@smartput/range-core` | The package root. |

## Runtime exports

Type-only exports are erased and do not appear here.

`DEFAULT_WEEK_START` · `InvertedRangeError` · `RANGE_WEIGHTS` · `WINDOWS` · `assertOrdered` · `endOfMonth` · `endOfWeek` · `endOfYear` · `resolveEndpoint` · `startOfMonth` · `startOfWeek` · `startOfYear` · `unwrapRange` · `wrapRange`

## Dependencies

- [`@smartput/core`](../core/README.md)
- [`@smartput/datetime`](../datetime/README.md)

## What it costs

Ceilings, not measurements. `bun run check-size` bundles each entry with
`bun build --minify` and fails if a row crosses its ceiling **or drops more
than 30 % below it** — a budget that is only an upper bound reports a vanished
graph as a triumph.

| Import | Minified | Gzipped |
| --- | --- | --- |
| range-core | ≤ 145 kB | ≤ 50.9 kB |

---

Generated by `scripts/gen-readmes.ts` — run `bun run docs:readmes`. Every
output above was produced by running the line beside it. The full page, with
live demos, is [`docs/packages/range-core.md`](../../docs/packages/range-core.md).
